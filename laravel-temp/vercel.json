{
  "version": 2,
  "buildCommand": "composer install --prefer-dist --no-dev --optimize-autoloader && npm install && npm run build",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "^/(.*)$",
      "dest": "/public/index.php"
    }
  ]
}
